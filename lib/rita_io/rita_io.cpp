// #include "rita_io.h"

// // Constructor: Initialize pin and set it as OUTPUT
// LedDriver::LedDriver(int pin) {
//     this->pin = pin;   // Store pin number
//     pinMode(pin, OUTPUT);
// }

// // Blinks LED a specified number of times
// void LedDriver::blink_led(int times, int on_time, int off_time) {
//     for (int i = 0; i < times; i++) {
//         digitalWrite(pin, HIGH);
//         delay(on_time);
//         digitalWrite(pin, LOW);
//         delay(off_time);
//     }
// }
